<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <title>MusicWorld</title>

    <!-- Bootstrap 4cdn -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <nav class="navbar navbar-dark bg-dark mb4">

    <a href="/" class ="navbar-brand">MusicWorld </a>
    </nav>
</head>

<body>

<!--Reference for ejs file https://github.com/FaztWeb/express-mongodb-crud-->
    <div class="container">

  <div class="row">
    <!--FORM-->
    <div class="col-md-5">
      <div class="card">
        <div class="card-body">
          <form action="/edit/<%= music._id %>" method="POST">
            <div class="form-group">
              <input type="text" name="name" placeholder="Name" class="form-control" value="<%= music.name %>">
            </div>
            <div class="form-group">
              <textarea name="description" cols="80"
                placeholder="Description"
                ><%= music.description %></textarea>
            </div>
            <button type="submit" class= "btn bun-success btn-block">
              update
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

</div>
</body>
</html>